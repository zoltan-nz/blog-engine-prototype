# Astro Supervisor

## Prerequisites

- Rust toolchain
- Node.js
- pnpm (npm install -g pnpm)
- create-astro (pnpm add -g create-astro)

## Local dev

```
cp .env.example .env
mise run dev-supervisor
```